# Railway provides DATABASE_URL automatically when you add Postgres
DATABASE_URL=postgresql://user:password@host:5432/onahealth

# Or use individual components for local development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=onahealth
POSTGRES_USER=onahealth
POSTGRES_PASSWORD=changeme

# JWT configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Server (Railway sets PORT automatically)
PORT=8000
