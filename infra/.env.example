# Cloud Platform
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=onahealth
POSTGRES_USER=onahealth
POSTGRES_PASSWORD=changeme

# JWT Secret for user auth
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Cloud API
CLOUD_HOST=0.0.0.0
CLOUD_PORT=8000

# Edge Agent
EDGE_HOST=0.0.0.0
EDGE_PORT=8080
CLOUD_API_URL=http://cloud:8000
DEVICE_TOKEN=dev-device-token

# Edge Storage
EDGE_DATA_DIR=/var/imaging-edge
SQLITE_DB_PATH=/var/imaging-edge/edge.db

# Sync Settings
SYNC_INTERVAL_SECONDS=30
SYNC_BATCH_SIZE=50
SYNC_MAX_RETRIES=5

# Model Settings
MODEL_DIR=/var/imaging-edge/models
DEFAULT_MODEL_VERSION=ona-cxr-tb-v1.0
